const Vacations = require('../models/Vacations')

const vacationsServices = {
    vacationsRequest: async({ id, dateStart, dateEnd }) => {
        // const request = Vacations.create({
        //     dateStart,
        //     dateEnd,
        //     emplooye: id
        // })
    }
}

module.exports = vacationsServices